import {Dimensions, StyleSheet} from 'react-native';
const {height, width} = Dimensions.get('screen');

const ProfileScreenStyle = StyleSheet.create({
    body: {
        backgroundColor: 'black',
        width: '100%',
        height: '100%'
    },
    
});

export {ProfileScreenStyle}